[package]
name = "policy-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Composable policy engine that chains compliance checks with AND/OR logic."
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
compliance-pausable = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
denylist-gate = { path = "../denylist-gate", features = ["testutils"] }
jurisdiction-flag = { path = "../jurisdiction-flag", features = ["testutils"] }

[features]
testutils = ["soroban-sdk/testutils"]
